import { RemovePuntoComaPipe } from './remove-punto-coma.pipe';

describe('RemovePuntoComaPipe', () => {
  it('create an instance', () => {
    const pipe = new RemovePuntoComaPipe();
    expect(pipe).toBeTruthy();
  });
});
