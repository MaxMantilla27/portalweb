import { RemovePortalCriterioPipe } from './remove-portal-criterio.pipe';

describe('RemovePortalCriterioPipe', () => {
  it('create an instance', () => {
    const pipe = new RemovePortalCriterioPipe();
    expect(pipe).toBeTruthy();
  });
});
